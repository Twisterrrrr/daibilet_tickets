# Admin V3 — аудит UX/UI (по гайдлайнам)

Основа: [`.cursor/rules/UX-UI-Code-Guidelines.mdc`](../.cursor/rules/UX-UI-Code-Guidelines.mdc), [admin-v3-ux.md](./admin-v3-ux.md).  
Область: `packages/frontend-admin-v3` (полный обход по grep + выборочное чтение экранов и общих компонентов).  
Дата: 2026-04-17.

---

## Сводка

| Категория | Оценка | Комментарий |
|-----------|--------|-------------|
| Визуальная иерархия, каркас страницы | Хорошо | `PageHeader`, `max-w-content`, секции `space-y-6`, карточки |
| Интерактивность кнопок (hover/focus/disabled) | Хорошо | `Button` (cva): `transition`, `focus-visible:ring`, `disabled:opacity-50` |
| Семантика кнопок vs div | В целом хорошо | Кликабельные действия — `Button`; `label`+checkbox для фильтров |
| Touch / размер целей | Риск | Почти везде `size="sm"` → **h-8 (32px)** &lt; рекомендуемых 44px |
| Загрузка контента | Средне | `LoadingState` — лёгкий pulse, не полноценный skeleton экрана |
| Таблицы на узких экранах | Средне | Есть `overflow-x-auto` + `min-w-[…]px`; **нет** подсказки «прокрутите» / тени края |
| Один primary CTA | Средне | На детальных страницах часто **несколько** `default` подряд (публикация / сохранение) |
| Шапка (Topbar) | Риск UX/a11y | Клик по «пустому» месту сворачивает шапку; **не дублируется** с клавиатуры |
| Формы: labels | Хорошо (пример) | `LoginPage`: `label` + `htmlFor` |
| Формы: JSON/админ-поля | Допустимо | `LandingDetailPage`: длинные `placeholder` как подсказка формата — label всё же нужен сверху |
| Motion (модалки) | Хорошо | Radix Dialog: `animate-in`, `duration-200` на контенте |

---

## Сильные стороны

1. **Оболочка** (`AdminShell`): ограничение ширины (`max-w-content`), ритм `px-6 pb-16 pt-8`, `animate-in-page`.
2. **Кнопка** (`components/ui/button.tsx`): единые состояния, фокус для клавиатуры.
3. **Навигация**: `NavLink` для пунктов меню, активное состояние читаемо; боковая панель с `aria-label`.
4. **Списки**: единый паттерн `FilterBar` + `DataTableShell` + таблица в `overflow-x-auto` (напр. `VenuesListPage`).
5. **Feature flags** (`SettingsPage`): черновик в локальном state, чекбокс реагирует сразу; сохранение отдельной кнопкой — предсказуемо.
6. **Фильтры**: чекбоксы через `<label className="… cursor-pointer">` — нормальная семантика.

---

## Замечания по категориям

### 1. Размер тач-целей и `size="sm"`

Компонент `Button`: `sm` = `h-8` + `text-xs` (`button.tsx`). По проекту **сотни** вхождений `size="sm"` в списках, табах, пагинации.

- **По гайдлайну**: интерактив не меньше ~44×44px на мобильных.
- **Факт**: 32px — плотная «админская» сетка, но для тача и доступности — отклонение.

**Рекомендация (системная, не точечная правка):**

- Ввести размер **`md` по умолчанию** для критичных действий на мобильном breakpoint **или**
- Добавить вариант `size="touch"` / расширить `sm` до `min-h-10`/`h-11` только в `@media (max-width: …)` **или**
- Оставить как есть для desktop-first админки, но **явно зафиксировать** в гайдлайне исключение «внутренний инструмент, приоритет плотность».

### 2. Topbar: клик по `header`

`Topbar.tsx`: у `<header>` висит `onClick` для сворачивания при клике вне интерактивных элементов.

- Плюс: не ломает кнопки (`closest` к `button,a,…`).
- Минусы: **нет клавиатурного эквивалента** для «клик по пустоте»; для скринридеров область «кликабельного пустого header» неочевидна; на таче случайные нажатия по шапке возможны.

**Рекомендация:** убрать клик с `header` и оставить только явную кнопку «свернуть шапку»; либо отдельная кнопка «Развернуть» в свернутом режиме без клика по всей полосе.

### 3. Таблицы

- Паттерн `overflow-x-auto` + большой `min-w` — **соответствует** «не сжимать таблицу в крошки».
- Нет **визуальной подсказки** «контент продолжается» (fade/shadow на краю) — из гайдлайна §9.

**Рекендация:** опционально обернуть общий скролл-контейнер (например расширение `DataTableShell` или общий `TableScrollRegion`).

### 4. Загрузка

`LoadingState`: короткий текст + 2 полоски pulse — не **skeleton** под структуру страницы.

**Рекомендация:** для тяжёлых списков — skeleton строк/карточек; для форм — скелет полей (по мере появления страниц с длинным TTFB).

### 5. Иерархия CTA

На детальных экранах (`CollectionDetailPage`, `LandingDetailPage`, `EventDetailPage`) часто рядом **несколько** кнопок уровня primary (`default`) или конкурирующие «Сохранить» / «Опубликовать».

**Рекомендация:** явно назначать **одну** primary на блок; остальное — `secondary` / `outline` (уже частично так, но не везде).

### 6. Мелкий текст и контраст

- В таблицах часто `text-xs` для заголовков колонок и ячеек — для **вторичной** информации допустимо; следить, чтобы `text-muted-foreground` не использовался для длинного чтения body.
- Бейдж инбокса в сайдбаре: `text-[10px]` — ок для счётчика, не для основного текста.

### 7. Поля без видимого label (кроме placeholder)

- `SettingsPage` (SEO): подписи как `text-xs text-muted-foreground` над полем — **labels есть**, но визуально слабее; для ключей вроде `siteName` можно усилить читаемость.
- `FeatureFlagsPanel`: колонка Description — `Input` без отдельного `<label>`, есть placeholder — **слабее** гайдлайна §4.

### 8. Оптимистичный UI

- Feature flags: локальный draft — ок.
- Мутации списков (одобрение отзыва, рефанд): состояние через `disabled` на кнопках, без оптимистичного обновления строк — **нормально** для финансово-чувствительных действий.

### 9. 8pt grid

В разметке много Tailwind-отступов `gap-2`, `gap-3`, `p-5`, `py-1.5` — часть из них **не кратна 8** в px (из-за шкалы Tailwind). Это **не баг**, но при жёстком аудите гайдлайна — отметить как «смешанная шкала проекта (4/8)».

---

## Приоритетный бэклог (предложение)

| Приоритет | Действие |
|-----------|----------|
| P1 | Topbar: убрать или переработать клик по всей шапке; явное управление сворачиванием |
| P1 | Политика размеров кнопок: либо touch-friendly breakpoint, либо зафиксированное исключение в гайдлайне |
| P2 | Общий scroll-hint для широких таблиц |
| P2 | Skeleton для «тяжёлых» первых загрузок списков |
| P3 | Пройтись по экранам с 2+ primary и выровнять варианты |
| P3 | Добавить явные `<label>` для полей в таблице feature flags (колонка Description) |

---

## Связанные файлы (эталоны для ревью)

- Оболочка: `app/layout/AdminShell.tsx`, `Sidebar.tsx`, `Topbar.tsx`
- Кнопка: `components/ui/button.tsx`
- Таблица-оболочка: `components/shared/table/DataTableShell.tsx`
- Состояния: `components/shared/states/LoadingState.tsx`, `ErrorState.tsx`
- Примеры списков: `modules/venues/pages/VenuesListPage.tsx`, `pages/events/EventsListPage.tsx`
- Настройки: `modules/settings/pages/SettingsPage.tsx`

---

*Документ можно обновлять после целевых рефакторингов (Topbar, Button sizes, table shell).*
