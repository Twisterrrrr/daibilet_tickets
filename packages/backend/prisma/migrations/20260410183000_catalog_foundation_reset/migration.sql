-- Catalog foundation reset (NO-OP).
--
-- Эта миграция изначально была задумана как \"foundation reset\" для чистой базы,
-- но находится в линейной истории миграций. Prisma применяет миграции на shadow DB
-- с нуля для валидации, и reset-миграция конфликтует с уже созданными таблицами/enum.
--
-- Поэтому в обычной истории миграций она должна быть безопасным no-op.
SELECT 1;

