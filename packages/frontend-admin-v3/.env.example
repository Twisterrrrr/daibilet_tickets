# База API (без хвостового /). В dev по умолчанию: /api/v1 (Vite проксирует на backend).
# VITE_API_BASE_URL=http://127.0.0.1:4000/api/v1

# Публичный URL сайта (опционально, например ссылки «как на сайте» в превью).
# VITE_PUBLIC_SITE_URL=https://example.com
