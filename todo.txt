есть план по улучшению производительности и безопасности. 
не надо сейчас делать, можно задокументировать


1) Рост БД и производительность PostgreSQL
Что будет расти быстрее всего

EventSession (расписания) — основной “пожиратель” строк.

ProcessedWebhookEvent, PaymentIntent, CheckoutSession — при платежах.

Audit, Logs, ReviewRequest, ReviewVote — при росте UGC/операций.

Что сделать заранее

План хранения EventSession

Хранить “будущее + недавнее прошлое”, а не всё бесконечно.
Практика: держать 90–180 дней истории сессий, остальное — архив/удаление (в зависимости от нужд аналитики).

Партиционирование для самых больших таблиц

EventSession по месяцу startsAt — почти всегда окупается при росте.

Таблицы “событийных логов” (webhooks/audit) — тоже по времени.

Индексы и планы запросов

Индексы должны соответствовать реальным фильтрам каталога: cityId + category + dateMode + startsAt, offerId + startsAt, slug unique, isDeleted, и т.п.

Регулярно: EXPLAIN ANALYZE на ТОП-10 запросов (каталог, детальная, поиски, админ-листинги).

Пул соединений

PgBouncer (transaction pooling), иначе на VDS легко упереться в connection limits, особенно с Next SSR и админкой.

Миграции

Только идемпотентные/безопасные миграции в production (у тебя это уже частично решено). 

Diary

2) Кэш, очереди, “горячие” места
Кэш

Определи строгий контракт: что кэшируем

events:detail:{slug} / venues:detail:{slug} — да.

Каталог — осторожно: лучше кэшировать частями (например, “top popular in city/category” на 5–10 минут).

Инвалидация

Инвалидация должна происходить из сервисов, которые меняют данные (sync, admin override, price updates). Ты уже двигаешься в эту сторону. 

OpenQuestions

Очереди (BullMQ)

У тебя уже закрыты ключевые риски: dedup jobId, concurrency=1, lockDuration, hard timeout. 

Diary


Дальше важно:

DLQ/failed jobs

Страница/endpoint в админке: список упавших sync/fulfillment job, кнопка retry.

Ограничение внешних API

Rate limiting на вызовы TC/Teplohod, backoff и таймауты (AbortController для отмены зависших HTTP).

3) Оптимизация процессов разработки и эксплуатации
Observability (обязательно до масштабирования)

Sentry/алерты

5xx, платежные ошибки, рост failed jobs, рост timeouts.

Метрики

p95 времени ответа каталога и детальной

время sync full/incremental

доля событий без priceFrom/без фото/без классификации (это продуктовая метрика качества)

Релизы

CI/CD

Авто-деплой с миграциями и rollback планом.

Фича-флаги

Выключать проблемные источники/типы purchaseFlow по городам без релиза.

Data quality pipeline

Отдельные “валидаторы наполненности”:

города без description не показывать (у тебя это уже правило) 

Tasktracker

события без image/priceFrom — в отдельный список на добивку

подозрительная категоризация — отчёт в админке (ты это планируешь)

4) Безопасность
Секреты и доступы

Секреты

Только env + secret manager (если появится), никаких ключей в репозитории.

Ротация: YooKassa, webhook secrets, API keys поставщиков.

API keys

Хэширование ключей (у тебя уже задумано) + префиксы для отображения.

IP allowlist для B2B API, rate limit.

OWASP минимум

CSRF/Clickjacking для админки

Helmet, CORS строго

Rate limit на публичные endpoints (особенно search, checkout init, review submit)

Защита от brute-force login (admin/supplier)

Платежи и вебхуки

Идемпотентность webhooks (у тебя есть) + хранение raw payload/traceId.

Строгая проверка подписи webhooks.

Отдельный “reconciliation” интерфейс — ты уже развиваешь это. 

Diary

Персональные данные

Минимизировать хранение: email/телефон только где нужно.

Отдельная политика retention: например, review requests и логи не хранить бесконечно.

Маскирование в логах (никаких токенов/паролей/полных email).

5) Что обычно “взрывается” первым

EventSession разрастается и начинает тормозить каталог

Решается: партиции + правильные индексы + хранить ограниченную историю.

Слишком много соединений к Postgres

Решается: PgBouncer + ограничение concurrency SSR.

Кэш рассинхронизируется

Решается: единая стратегия инвалидации и TTL как страховка.

Поддержка тонет из-за “пограничных” кейсов платежей/сертификатов

Решается: админ-инструменты resend/retry + статусная модель.


разбиваем на несколько стадий:

0) Правила работы (обязательные)

Один PR = одна тема. Никаких “заодно поправил…”.

Каждая задача имеет DoD (Definition of Done) и “область влияния” (какие модули трогаем).

Никаких изменений в платежном контуре без отдельной задачи.

Каждый PR должен быть проверяем: тесты + 5–10 ручных smoke-check шагов.

Batch A — Observability baseline

A1. requestId + корреляция логов

Scope: backend middleware/interceptor + logger

DoD:

каждый запрос имеет requestId

requestId в логах NestJS

маскирование email/phone/token в логах

A2. Sentry (или аналог) + алерты

DoD:

captureException работает

5xx/timeout/payment-error тэгируются

включено только в production env

A3. Ops endpoints

/admin/ops/health (stale pending, failed unresolved, active jobs)

/admin/ops/metrics (rates/thresholds)

DoD: эндпоинты доступны только admin-ролью

Batch B — Security baseline

B1. Helmet + security headers

DoD: включены базовые заголовки, CSP не ломает фронт

B2. CORS policy

DoD: разрешены только нужные origins (prod/stage), preflight ок

B3. Rate limiting

Публичные: search, checkout init, review submit, auth endpoints

Admin: login

DoD: лимиты конфигурируемые через env, есть unit-тест/интеграционный тест на 429

B4. Brute-force защита

DoD: блокировка/замедление после N попыток, логирование

Batch C — Queue/Jobs hardening

C1. DLQ / Failed jobs visibility

Admin страница/эндпоинт: failed jobs list (sync/fulfillment)

DoD: видно jobId, attempts, stack, кнопка retry

C2. HTTP timeouts + AbortController

Scope: tc/tep api clients

DoD: все внешние HTTP имеют timeout; при job timeout запросы отменяются

C3. Rate limit внешних API

DoD: ограничение concurrency/requests per sec на TC/TEP, backoff

Batch D — Cache discipline

D1. Единый CacheService контракт

DoD: get/set/delete, namespace ключей, TTL по умолчанию

D2. Инвалидация по источникам изменений

Admin override save → invalidate detail

Sync update → invalidate detail (+ при необходимости list shards)

DoD: документированная матрица “событие → какие ключи сбрасываем”

D3. TTL policy

DoD: detail TTL (например 1–6ч), list TTL (5–10 мин), fallback

Batch E — DB growth & retention (без партиционирования)

E1. Retention jobs

EventSession: хранить только future + N дней назад

ProcessedWebhookEvent, Audit: TTL/архив

DoD: cron/queue job + dry-run режим + отчёт

E2. Индексы под реальные запросы

DoD: миграция idempotent + EXPLAIN на топ-запросы каталога/детальной

E3. PgBouncer (опционально)

DoD: docker-compose + env + проверка лимита соединений

Batch F — DB partitioning (последним)

F1. План миграции

DoD: документ “как включаем” + rollback

F2. Партиционирование EventSession

DoD: новые партиции создаются автоматически, запросы используют pruning, тест на insert/select

3) Стандартный чеклист DoD для каждого PR

 Изменения только в пределах scope

 Добавлены/обновлены env.example и docs (если нужно)

 Все тесты проходят

 Линтер/типизация проходят

 Нет утечек секретов/PII в логах

 Нет изменения контрактов API без версии/обратной совместимости

 Smoke-check выполнен

4) Smoke-check (универсальный, 10 минут)

/events (3 категории), фильтры, сортировки

/events/[slug] и /venues/[slug]

админка: логин, список событий, редактирование override

sync: incremental вручную (или endpoint)

checkout sandbox (если трогали оплату)

проверка логов: requestId, отсутствие PII

проверка rate limit: 429 на перегруз

health/metrics endpoints доступны только админу

лучше выделить каждый этап A1-F2 как отдельную задачу:

Формат одинаковый: цель → scope → non-goals → DoD → tests → smoke.

A1 — requestId + корреляция логов + маскирование PII

Цель: добавить requestId для каждого HTTP-запроса, прокинуть в логи, замаскировать PII (email/phone/tokens).
Scope: backend: middleware/interceptor, logger util, config env.
Non-goals: не менять бизнес-логику, не трогать платежный контур.
DoD:

requestId генерируется если отсутствует (UUID/shortid), также читается из x-request-id если есть

requestId добавляется в response header x-request-id

requestId присутствует во всех логах запросов и ошибок

маскирование: email, phone, authorization, cookie, token, apiKey (в логах и error context)

документация: где смотреть requestId
Tests: unit на mask util; e2e: запрос → header есть; ошибка → requestId в логе.
Smoke: открыть 2 страницы фронта, вызвать 404, убедиться что requestId в логах.

A2 — Sentry baseline (или текущий аналог)

Цель: подключить Sentry (или уже выбранный error tracker) для backend, с тегами окружения и requestId.
Scope: backend bootstrap + exception filter/interceptor.
Non-goals: не менять обработку платежей.
DoD:

включается только при SENTRY_DSN и NODE_ENV=production

ошибки 5xx репортятся, 4xx не репортятся (кроме whitelist)

теги: env, service, requestId, route, userId (если есть)
Tests: unit на фильтр (4xx не отправлять).
Smoke: искусственно выбросить ошибку → событие уходит.

A3 — Ops endpoints: health/metrics (admin-only)

Цель: добавить admin-only эндпоинты /admin/ops/health и /admin/ops/metrics.
Scope: backend admin module/controllers/services.
Non-goals: не трогать данные платежей, кроме read-only агрегаций.
DoD:

/admin/ops/health: pending stale, failed unresolved, escalated open, active intents, active sync jobs (если есть)

/admin/ops/metrics: rates (fulfillment_fail_rate, webhook_dedup_rate, auto_compensate_rate) и thresholds

оба эндпоинта защищены admin guard
Tests: e2e admin auth required.
Smoke: открыть админку → endpoints отвечают.

B1 — Helmet + security headers

Цель: включить Helmet и базовые security headers без поломки фронта/виджетов.
Scope: backend main.ts/app bootstrap.
Non-goals: не включать жёсткий CSP, если ломает teplohod/TC.
DoD:

включены стандартные headers

настроены исключения/allowlist для нужных embed скриптов (если CSP включается)

документация: какие домены разрешены
Tests: e2e: основные страницы грузятся.
Smoke: открыть страницу с виджетом teplohod/TC.

B2 — CORS policy

Цель: строго ограничить CORS по окружениям.
Scope: backend bootstrap + env.
Non-goals: не ломать local dev.
DoD:

CORS_ORIGINS env (comma-separated)

dev: разрешить localhost (явно)

prod: только daibilet.ru + admin domain (если отдельный)

preflight ok
Tests: unit на парсинг origins; e2e preflight.
Smoke: фронт/админка работают.

B3 — Rate limiting на публичные и admin login

Цель: добавить rate limit на чувствительные endpoints.
Scope: backend guards/middleware + config.
Non-goals: не ограничивать webhooks provider (для них отдельные лимиты).
DoD:

limits в env (public/auth/admin/webhook)

429 с понятным error code

отдельные лимиты: /api/*search*, /checkout/*, /admin/auth/login, /reviews/*
Tests: e2e: 429 после N запросов.
Smoke: руками дернуть endpoint 20 раз → 429.

B4 — Brute-force защита admin login

Цель: защитить admin login от перебора (IP + username).
Scope: admin auth controller/service.
Non-goals: не менять JWT/пароли.
DoD:

после N попыток: cooldown (например 10 минут) или progressive delay

логирование события (без раскрытия пароля)

админ видит “слишком много попыток”
Tests: unit на счетчик.
Smoke: 6 неверных логинов → блок/задержка.

C1 — Failed jobs visibility + retry (Admin)

Цель: страница/эндпоинт для failed jobs (sync/fulfillment) и кнопка retry.
Scope: admin backend + admin frontend.
Non-goals: не менять логику job processing.
DoD:

список jobs: name, jobId, attemptsMade/attempts, failedReason, stack (short), timestamp

action: retry (с аудитом кто нажал)

фильтр по queue name
Tests: unit на service; e2e retry endpoint admin-only.
Smoke: искусственно завалить job → увидеть → retry.

C2 — HTTP timeouts + AbortController для TC/TEP

Цель: все внешние HTTP-запросы имеют timeout и поддерживают отмену при job timeout.
Scope: tc/tep api clients, sync services.
Non-goals: не менять формат данных.
DoD:

timeout конфигурируемый через env

AbortController прокидывается до fetch/axios

при job timeout вызывается abort
Tests: unit: abort triggers; integration: simulated hang.
Smoke: поставить timeout=1s на dev → убедиться что запросы не висят.

C3 — Rate limiting внешних API (TC/TEP)

Цель: не бомбить поставщиков; стабилизировать sync.
Scope: sync services.
Non-goals: не менять расписание cron.
DoD:

concurrency limit (например p-limit)

backoff на 429/5xx

метрика времени sync и количества retries
Tests: unit на limiter.
Smoke: full sync в dev → без всплесков.

D1 — Единый CacheService контракт + namespacing

Цель: централизовать ключи кэша и TTL.
Scope: backend cache module/service.
Non-goals: не менять данные в БД.
DoD:

get/set/del + delByPrefix (если Redis)

ключи через helper: cacheKeys.events.detail(slug) и т.п.

default TTL
Tests: unit на key builder.
Smoke: детальная страница кэшируется.

D2 — Инвалидация кэша по изменениям (override/sync/admin)

Цель: гарантировать актуальность детальных страниц и ключевых листингов.
Scope: event-override.service, sync services, admin update flows.
Non-goals: не внедрять сложный list-cache, только необходимые ключи.
DoD:

при сохранении override: del detail по slug

после sync update: del detail по slug (и venue detail если затронуто)

матрица “что сбрасываем” в docs
Tests: integration: update override → cache miss next.
Smoke: поменять title override → сразу видно на фронте.

D3 — TTL policy (detail/list)

Цель: установить единые TTL и fallback.
Scope: cache config.
Non-goals: не менять логику каталога.
DoD:

detail TTL 1–6h

list/top TTL 5–10m (если есть)

TTL в env
Tests: unit parse env.
Smoke: убедиться, что ключи с TTL.

E1 — Retention jobs (cleanup)

Цель: контролировать рост таблиц (sessions/webhooks/audit).
Scope: backend cron/queue jobs + admin report.
Non-goals: не трогать бизнес-таблицы без явного окна хранения.
DoD:

env: RETENTION_EVENTSESSIONS_DAYS, RETENTION_WEBHOOK_DAYS, RETENTION_AUDIT_DAYS

dry-run mode: отчёт сколько строк удалится

реальные удаления батчами (limit)
Tests: unit dry-run; integration delete batches.
Smoke: прогнать dry-run, проверить отчёт.

E2 — Индексы под реальные запросы

Цель: добавить недостающие индексы под /events, /catalog, /venues, admin list.
Scope: миграции (idempotent), prisma schema if needed.
Non-goals: не менять бизнес-логику.
DoD:

миграция(и) безопасные: IF NOT EXISTS

список индексов в docs

EXPLAIN на 3 ключевых запросах до/после (в комментарии PR)
Tests: migrate deploy на чистой и на существующей БД.
Smoke: каталог быстрее (по логам/метрике).

E3 — PgBouncer (опционально, если уже в планах)

Цель: ограничить количество соединений и стабилизировать SSR/админ.
Scope: infra/docker + backend connection string.
Non-goals: не менять Postgres.
DoD:

pgbouncer поднимается

backend подключается через него в prod compose

параметры пула документированы
Tests: healthcheck.
Smoke: проверить max connections.

F1 — План миграции партиционирования

Цель: подготовить документированный план внедрения без простоя.
Scope: docs + миграции-подготовки.
Non-goals: не включать партиции без готового плана отката.
DoD:

документ: steps, rollback, verification queries

подготовлены скрипты создания партиций
Tests: dry-run на staging (если есть).
Smoke: checklist выполнен.

F2 — Партиционирование EventSession по startsAt (месяц)

Цель: ускорить запросы и ограничить рост.
Scope: Postgres migrations + prisma raw SQL.
Non-goals: не менять API.
DoD:

родительская таблица + партиции по месяцам

функция/cron для создания будущих партиций на N месяцев вперёд

подтверждение pruning на EXPLAIN
Tests: insert/select, migrate deploy idempotent.
Smoke: каталог по датам работает.